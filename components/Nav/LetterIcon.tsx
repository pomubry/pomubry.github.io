import { Heading } from "@chakra-ui/react";

const LetterIcon = () => {
  return (
    <Heading pt={2} size="2xl" flex={1} userSelect="none">
      BT
    </Heading>
  );
};

export default LetterIcon;
